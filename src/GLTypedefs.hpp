#pragma once

#include "GLApi.hpp"

using GLHandle = GLuint;
using GLResult = GLint;

using GLBufferHandle = GLHandle;
using VbHandle = GLBufferHandle;
using IbHandle = GLBufferHandle;

using ShaderProgram = GLHandle;
using ShaderHandle = GLHandle;
using ShaderLocation = GLHandle;
